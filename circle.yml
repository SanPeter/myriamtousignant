test:
  pre:
    - hexo clean
    - hexo generate
    - hexo server --port 8082
    - curl --retry 10 --retry-delay 2 -v http://localhost:8082
  override:
    - backstop test
    - mkdir -p $CIRCLE_TEST_REPORTS
    - cp ./backstop_data/ci_report/* $CIRCLE_TEST_REPORTS/